POSTGRES_USER=snake
POSTGRES_PASSWORD={{ postgres_password }}
POSTGRES_DB=snakegame

JWT_SECRET_KEY={{ jwt_secret_key }}

# Serve everything (frontend + proxied API) on the standard HTTP port, so
# the app is reachable at just http://<server-ip> with no port needed.
FRONTEND_PORT=80
